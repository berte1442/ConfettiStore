@model IEnumerable<ConfettiStore.Models.OrderAttachment>

@{
    ViewData["Title"] = "Index";
}
    <div class="d-none d-md-block">
        <i class="fas fa-link text-dark float-right" style="font-size:50px;"></i>
        <ul class="breadcrumb">
            <li><a href="#" onclick="location.href='@Url.Action("Index", "Home")'">Home</a></li>
            <li>Order Attachments</li>
        </ul>
    </div>
    <div class="navbar sticky-top backgroundColor navbar-position">
        <div class="container">
            <h1 class="opacity d-none d-sm-block"><i class="fas fa-link text-dark" style="font-size:30px;"></i> Order Attachments</h1>
            <h2 class="opacity d-xs-block d-sm-none"><i class="fas fa-link text-dark" style="font-size:25px;"></i> Order Attachments</h2>
        </div>
        <div class="d-xs-none d-lg-block">
            <div class="d-xs-none d-lg-block">
                <div class="input-group">
                    <form id="orderAttachment-search-note">
                        <input class="form-control py-2 border-right-0 border" type="text" placeholder="Search..">
                    </form>
                    <span class="input-group-append">
                        <a class="search-filter input-group-text py-2 border-left-0 opacity"><i class="fa fa-search "></i></a>
                    </span>
                </div>
            </div>
        </div>
        <div class="@*col-sm-2*@ justify-content-end">
            <button class="btn btn-success opacity" type="button" onclick="location.href='@Url.Action("Create", "OrderAttachments")'">Create New</button>
        </div>
    </div>
<div class="table-responsive">
    <table id="orderAttachmentsTable" class="table opacity">
        <caption>Order Attachments</caption>
        <thead class="thead-light">
            <tr>
                <th id="date" onclick="sortTable(0, id)" scope="col" class="d-none d-lg-table-cell">
                    @Html.DisplayNameFor(model => model.Created)
                </th>
                <th id="int" onclick="sortTable(1, id)">
                    @Html.DisplayNameFor(model => model.Order)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody id="orderAttachment-tableBody">
            @foreach (var item in Model)
            {
                <tr>
                    <td scope="col" class="d-none d-lg-table-cell font-weight-bold">
                        @Html.DisplayFor(modelItem => item.Created)
                    </td>
                    <td class="font-weight-bold">
                        @Html.DisplayFor(modelItem => item.Order.OrderId)
                    </td>
                    <td align="right">
                        <a asp-action="Details" asp-route-id="@item.OrderAttachmentId"><i class="fas fa-edit"><label class="d-none d-md-block">Select</label></i></a><br />

                        @*<a asp-action="Edit" asp-route-id="@item.OrderAttachmentId"><i class="fas fa-edit"><label class="d-none d-md-block">Edit</label></i></a><br />
                        <a asp-action="Details" asp-route-id="@item.OrderAttachmentId"><i class="fas fa-info"><label class="d-none d-md-block">Info</label></i></a><br />
                        <a asp-action="Delete" asp-route-id="@item.OrderAttachmentId"><i class="fas fa-trash"><label class="d-none d-md-block">Delete</label></i></a>*@
                    </td>
                </tr>
            }
        </tbody>
    </table>
 </div>
<div class="navbar fixed-bottom backgroundColor">
    <button class="btn btn-primary d-xs-block d-lg-none" type="button" onclick="location.href='@Url.Action("Index", "CustomerOrders")'"><i class="fas fa-chevron-left"></i></button>
    <button class="btn btn-primary d-xs-block d-lg-none" type="button" onclick="location.href='@Url.Action("Index", "OrderItems")'"><i class="fas fa-chevron-right"></i></button>
</div>
